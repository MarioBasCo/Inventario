import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stip.appinventario',
  appName: 'AppInventario',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
